#include <stdio.h>
#include <stdlib.h>

int main()
{
    int contador = 0;
    /* while (contador < 10)
    {
        printf("%d \n" , (contador +1));
        contador ++;
    }
    */

    for(contador = 0; contador <10; contador++)
        {
        printf("%d\n", contador+1);
        }
    return 0;
}
